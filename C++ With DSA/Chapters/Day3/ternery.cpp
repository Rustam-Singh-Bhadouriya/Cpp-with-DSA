#include <iostream>
using namespace std;

int main() {
    // ternery statment -> codition ? stt1 : stt2;
    // stt1 -> True statment and stt2 is false statment
    int n = -9;
    // use () for any mathmatical or oprator opration in cout
    cout<< (n >= 0 ? "Positive" : "Negative");
    return 0;
}